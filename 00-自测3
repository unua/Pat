#include<stdio.h>

int list[100];

void revers(int *lis,int p,int q){
	int temp;
	while(p<q){
		temp=lis[p];
		lis[p]=lis[q];
		lis[q]=temp;
		p++;q--;
	}
}

int main(){
	int m,n;
	while(scanf("%d%d",&n,&m)!=EOF){
		for(int i=0;i<n;i++){
			scanf("%d",&list[i]);
		}
		
		m=m%n;
		revers(list,0,n-1);
		revers(list,0,m-1);
		revers(list,m,n-1);
		
		for(int i=0;i<n;i++){
			if(i==0)printf("%d",list[i]);
			else printf(" %d",list[i]);
		}
	}
	return 0;
}

//M>N? 
